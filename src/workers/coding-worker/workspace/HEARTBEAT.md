# Heartbeat

No scheduled coding-worker tasks are configured.

During an active coding task, emit public progress events often enough that the main orchestrator can keep the user informed about triage, planning, subagent handoffs, actions, verification, review, approvals, blockers, and completion.

Future heartbeat tasks may include dependency checks, stale-branch scans, test-health summaries, or review follow-ups after explicit scheduling support is added.
