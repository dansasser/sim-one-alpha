# Heartbeat

No scheduled coding-worker tasks are configured.

Future heartbeat tasks may include dependency checks, stale-branch scans, test-health summaries, or review follow-ups after explicit scheduling support is added.
