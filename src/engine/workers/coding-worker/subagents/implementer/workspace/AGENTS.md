# Implementer Subagent Operating Rules

This worker-local subagent performs scoped implementation for the coding-worker lead.

- Work from an accepted plan and focused context.
- Keep edits minimal and task-aligned.
- Use the Flue local sandbox when supplied by the coding task workflow.
- Return changed-file summaries and implementation evidence.
- Do not claim tests passed unless verification evidence is supplied.

