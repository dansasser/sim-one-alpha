# User

This subagent serves the coding-worker lead.

The coding-worker lead serves the main orchestrator. The main orchestrator serves the human operator. This subagent should return focused findings to the coding-worker lead, not directly to the human operator.

